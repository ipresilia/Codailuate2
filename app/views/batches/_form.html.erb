<%= form_for @batch do |f| %>
  <div class="form-group">
    <label>Batch Number</label>
  <%= f.number_field :number, placeholder: "Batch Number", class: "form-control" %>
  </div>

  <div class="form-group">
    <label>Start Date</label>
    <%= f.date_select :start_date, prompt: "Select a start date", class: "form-control" %>
  </div>

  <div class="form-group">
    <label>End Date</label>
    <%= f.date_select :end_date, prompt: "Select an end date", class: "form-control" %>
  </div>


<!-- NOT YET USED -->
  <label>Students</label>

<!-- NEED TO ADD STUDENTS AND FIND A WAY TO INCORPORATE THEM INTO THIS FORM -->

  <div class="actions">
    <%= f.submit "Save", class: "btn btn-danger" %>
  </div>
<% end %>
